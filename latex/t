113292475659649136992	1456600615851	Initialise UMI	When you introduce notation you need to first declare the variables, terms and well formed formulas that will be used in the definition. \nTo do this first list the variables in the form $\\forall x \\forall y \\forall fred \\forall etc$, \nthen declare the type, that is either, $\\text{Var}$ for variable, $\\text{Term}$ for terms and $\\text{Wff}$ for well formed formulas, in the form of conjunctions. Then you use the $\\rightarrow$ symbol, \nthen you declare the notation using the $:=$ predicate (you can use $:=$ in infix notation style if you have {\\id=0 Alias Alias} as a dependency))\n\\par\nA good simple example is {\\id=8 Set Intersection}, that is the $\\cap$ symbol. (Remember you can right click on maths to view the LaTeX).
113292475659649136992	1456600615875	Initialise UMI	@@ -7,0 +7,4 @@\n+\n+\\section{Notational Conflicts}\n+\n+At the moment try to avoid defining identical notational patterns, for example defining $+$ on both numbers and vectors.  This is because we have not yet fully implemented a type system that can resolve the ambiguity (but are very close).
113292475659649136992	1456600616291	Initialise UMI	@@ -1,2 +1,2 @@\n-When you introduce notation you need to first declare the variables, terms and well formed formulas that will be used in the definition. \n-To do this first list the variables in the form $\\forall x \\forall y \\forall fred \\forall etc$, \n+When you introduce notation you need to first declare the (unbounded) variables, terms and well formed formulas that will be used in the definition. \n+To do this first list the variables in the form $\\forall x \\forall y \\forall \\mathbb{G} \\forall etc$, \n@@ -4,1 +4,1 @@\n-then you declare the notation using the $:=$ predicate (you can use $:=$ in infix notation style if you have {\\id=0 Alias Alias} as a dependency))\n+then you declare the notation using the $:=$ predicate (you can use $:=$ in infix notation style if you have {\\id=0 Alias Alias} as a dependency)).\n@@ -6,0 +6,3 @@\n+The $:=$ symbol means \"means\", i.e. the thing on the left is the thing to be defined - the thing that has not yet had meaning assigned, \n+the thing on the right is the meaning - i.e. some sequence of symbols that have been defined.\n+\\par
113292475659649136992	1456600616312	Initialise UMI	@@ -6,2 +6,2 @@\n-The $:=$ symbol means \"means\", i.e. the thing on the left is the thing to be defined - the thing that has not yet had meaning assigned, \n-the thing on the right is the meaning - i.e. some sequence of symbols that have been defined.\n+The $:=$ symbol means \"means\". So the thing on the left is the thing to be defined i.e. the thing that has not yet had meaning assigned.\n+The thing on the right is the meaning i.e. some sequence of symbols that have been defined.
